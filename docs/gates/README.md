# Gate records

Gate records are evidence, not feature flags. Repository copies contain no
secrets, raw tokens, buyer data, or unredacted provider records. Human and live
procedures stay `not_run` until the named owner actually completes them.

Changing a protocol, measurement method, or schema invalidates dependent gates
until compatible regression evidence is recorded.
