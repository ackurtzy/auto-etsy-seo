{
  "name": "auto-etsy-seo-rebuild",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test:phase2": "node --test packages/engine/test/*.test.ts",
    "typecheck:phase2": "tsc -p tsconfig.phase2.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
